apiVersion: v1
kind: Service

metadata:
  name: todo-service
  namespace: todo-app
spec:
  type: NodePort

  selector:
    app: todo-app

  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30090